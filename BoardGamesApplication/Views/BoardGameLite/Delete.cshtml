@model BoardGamesDataAccess.Models.BoardGameLite

@{
    ViewBag.Title = "Delete";
}

<h2>Delete @Model.Name</h2>

<h3>Are you sure you want to delete @Model.Name?</h3>
<div>
    <div class="form-actions no-color">
        @using (Html.BeginForm())
        {
            <button onclick="location.href='@Url.Action("Delete", "BoardGameLite", new { gameId = Model.Id})';">Delete</button>
        }
    </div>
    <br />
    <button onclick="location.href='@Url.Action("Index", "BoardGameLite")';">Back to list</button>
</div>
